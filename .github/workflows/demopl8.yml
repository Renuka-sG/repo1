name: matrix strategy workflow
on:
  push:
  workflow_dispatch:


jobs:
  deploy:
    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-20.04, windows-latest]

    runs-on: ${{ matrix.os }}
    steps:
      - name: Echo Docker Details
        run: echo "${{ matrix.os }}"
        
